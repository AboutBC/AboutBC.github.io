{% comment %}
Manually sort work items: treat "Present" as far future date (e.g., 9999-12-31)
{% endcomment %}
{% assign work_entries = data[1] %}
{% assign work_entries = work_entries | map: "content" %}

{% assign sorted_work = work_entries | sort: "sortKey" %}
{% assign sorted_work = sorted_work %}

<ul class="card-text font-weight-light list-group list-group-flush">
  {% for content in work_entries %}
    {% assign endDateRaw = content.endDate | default: "9999-12-31" %}
    {% if endDateRaw = "" %}
        {% assign endDateRaw = "9999-12-31" %}
    {% endif %}
    {% assign sortKey = endDateRaw %}
    {% capture sortKey %}{{ endDateRaw | date: "%Y%m%d" }}{% endcapture %}
    {% assign content = content | merge: {"sortKey": sortKey} %}
  {% endfor %}

  {% assign sorted_work = work_entries | sort: "sortKey" %}

  {% for content in sorted_work %}
    <li class="list-group-item">
      <div class="row">
        <div class="col-xs-2 cl-sm-2 col-md-2 text-center date-column">
          {% assign startDateFormatted = content.startDate | date: "%b %Y" %}
          {% if content.endDate == "9999-12-31" or content.endDate == "" %}
            {% assign endDateFormatted = "Present" %}
          {% else %}
            {% assign endDateFormatted = content.endDate | date: "%b %Y" %}
          {% endif %}
          <table class="table-cv">
            <tbody>
              <tr>
                <td>
                  <span class="badge font-weight-bold danger-color-dark text-uppercase align-middle" style="min-width: 75px"> 
                    {{ startDateFormatted }} - {{ endDateFormatted }} 
                  </span>
                </td>
              </tr>
              {% if content.location %}
                <tr>
                  <td>
                    <p class="location">
                      <i class="fa-solid fa-location-dot iconlocation"></i>
                      {{ content.location }}
                    </p>
                  </td>
                </tr>
              {% endif %}
            </tbody>
          </table>
        </div>
        <div class="col-xs-10 cl-sm-10 col-md-10 mt-2 mt-md-0">
          <h6 class="title font-weight-bold ml-1 ml-md-4">
            <a href="{{ content.url }}">{{ content.position }}</a>
          </h6>
          <h6 class="ml-1 ml-md-4" style="font-size: 0.95rem">{{ content.name }}</h6>
          <h6 class="ml-1 ml-md-4" style="font-size: 0.95rem; font-style: italic">{{ content.summary }}</h6>
          <ul class="items">
            {% for item in content.highlights %}
              <li><span class="item">{{ item }}</span></li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </li>
  {% endfor %}
</ul>
